import styled from 'styled-components'

const Actions = styled.div`
  position: absolute;
  top: 2px;
  right: 0;
  z-index: 1;

  .edit {
    border-radius: 8px 0px 0px 8px;
  }
`

export { Actions }